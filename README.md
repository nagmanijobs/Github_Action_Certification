# Github_Action_Certification

Small change: add a line to create a commit on the `feature` branch.

## Sample hello-world app

A minimal HTML/CSS/JS sample lives in the `hello-world/` directory. Open `hello-world/index.html` in your browser or serve the repo with a simple static server (for example `python3 -m http.server 8000`) and visit:

http://localhost:8000/hello-world/